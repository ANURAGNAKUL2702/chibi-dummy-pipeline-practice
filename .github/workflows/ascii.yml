name: Generate ascii flow

on:
  push

jobs:
  ascii_job:
    runs-on: ubuntu-latest 
    steps: 
    - name: Checkout repo
      uses: actions/checkout@v4

    - name: executing shell scripting
      run: ./ascii.sh
      
